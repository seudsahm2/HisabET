import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors - Premium Professional Palette
  static const Color primary = Color(0xFF004D40); // Rich Teal (Money/Finance)
  static const Color primaryDark = Color(0xFF00251A);
  static const Color primaryLight = Color(0xFF39796B);

  static const Color accent = Color(0xFFFFD700); // Gold for premium touches

  // Semantic Colors (High Contrast)
  static const Color give = Color(0xFF2E7D32); // Success Green
  static const Color giveLight = Color(0xFFE8F5E9);

  static const Color take = Color(0xFFC62828); // Alert Red
  static const Color takeLight = Color(0xFFFFEBEE);

  // Surface Colors
  static const Color background = Color(0xFFFAFAFA); // Almost White
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF212121); // Charcoal
  static const Color textSecondary = Color(0xFF757575);
}
